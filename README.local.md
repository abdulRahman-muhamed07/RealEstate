# Local setup

The API requires SQL Server for the development database. The repository default points to `localhost` / `RealEstateDb`.

If your local SQL Server is a named instance, set `ConnectionStrings__DefaultConnection` to the correct server/instance before running the API.
